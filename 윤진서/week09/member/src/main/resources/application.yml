spring:
  application:
    name: member
  datasource:
    url: jdbc:mysql://localhost:3306/member
    username: root
    password: jinjin0171$
  jpa:
    hibernate:
      ddl-auto: update # 프로그램 껐다 켜도 데이터 유지되고 변경된 부분만 반영
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect # 사용할 데이터베이스 SQL 문법 지정
    show-sql: true # 실행되는 SQL을 콘솔에 출력할지 여부
